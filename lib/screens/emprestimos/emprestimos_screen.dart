import 'package:flutter/material.dart';

class EmprestimosScreen extends StatelessWidget {
  const EmprestimosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Emprestimos'));
  }
}